---
- name: Set full container image path
  set_fact:
    #full_image_path: "{{ registry_url }}/{{ container.image }}:{{ container.tag }}"
    full_image_path: dolvladzio/frontend

- name: Combine with common_container_env
  set_fact:
    combined_env: "{{ common_container_env | combine({ 'REACT_APP_API_BASE_URL': ansible_host }) }}"

- name: Create Docker container
  docker_container:
#    name: "{{ container.name }}"
    name: "frontend"
    image: "{{ full_image_path }}"
    state: started
    restart_policy: "{{ container_restart_policy }}"
    ports: "{{ container.ports }}"
    env: "{{ combined_env }}"
    volumes: "{{ container.volumes | default([]) }}"
    pull: yes
