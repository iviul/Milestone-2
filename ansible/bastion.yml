---
- name: Configure Bastion Host
  hosts: bastion
  become: true

  roles:
    - ssh-users
    - bastion-restriction

- name: Configure local config file
  hosts: localhost
  become: false

  roles:
    - configure-ssh-config